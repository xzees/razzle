import React, { FunctionComponent } from 'react';
import { BannerWrapper } from './Style';

const MenuBar: FunctionComponent<any> = (props: any) => {
  return <BannerWrapper />;
};

export default MenuBar;
