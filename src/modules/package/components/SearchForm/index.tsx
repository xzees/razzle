import React, { FunctionComponent } from 'react';

type Props = {
  [key: string]: any;
  Theme: any;
  btnSearch: any;
};

const SearchForm: FunctionComponent<Props> = ({ Theme, btnSearch }) => {
  return <></>;
};

export default SearchForm;
